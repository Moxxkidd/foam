# 已知问题总表(WP-13 收口,2026-08-27)

> 汇总口径:M3 门口挂账 9 条(①–⑨)+ 各 WP 日志遗留在册项(⑩–⑫)+
> WP-13 整合期新发现(⑬–⑭)。全部如实收录,逐条给 v1 去向;v1 立项清单
> 本体(含优先级排序)在 `docs/HANDOVER.md`「v1 立项清单」节。
> 本表不替代各 WP 开发日志的原始记录,证据以原始记录为准。

## A. M3 挂账(2026-08-26 门口移交,候选已全数并入 v1 清单)

| # | 问题 | 现象与证据 | 影响 | v1 去向 |
|---|---|---|---|---|
| ① | creds/vulns 仅经解析 facts 入库,state 工具面无 add_vuln/add_cred | 手工成果无处登记;场景一索引 hosts/ports/creds/vulns=0(WP-13 复核 sqlite 直查一致),场景二同缺口 | 手工/非注册工具产出进不了索引,报告完整性受损 | v1-P1:state 工具面补登记接口 |
| ② | claim-correction 启发式对「总结历史动作」稳定误触发 | 场景一 4 次(audit seq 44/49/52/54)、场景二 1 次(seq 41);多耗轮次与 token | 误报成本真实;预算机制挡死循环但浪费 | v1-P1:豁免无新动作声明的总结段 |
| ③ | 模型自截断输出(`\| tail -20`)使解析器 no_match | 场景一 seq 4–5 parse_fallback 实录;场景二未复现 | 解析增强被旁路,索引漏收 | v1-P1:prompts 引导扫描类命令保持完整输出 |
| ④ | PTY 会话操作(session_open/send/read/list)不入审计链 | 场景二 replay seq 16–33 纯会话阶段只剩 LLM 交换元记录 | replay 时间线在交互期断档;审计完整性缺口 | v1-P0:会话审计 kind(**门口裁决项**,M3 门口未定论,留 M4) |
| ⑤ | 结构化提示事件(events/matched)只喂模型不落盘 | WP-12 验收 3 引用靠运行日志+转录+离线复演补强 | 验收与事后核查证据链偏弱 | v1-P0(与④并案):提示事件落盘 |
| ⑥ | loot 索引登记尺寸为登记时刻快照 | 终态文件可被后续重拷撑大(WP-12 场景二转录二次拷贝实录) | 登记语义含糊 | v1-P1:语义定夺(**门口定夺项**) |
| ⑦ | 护栏目标提取把版本号(`4.7p1`/`3.2.8.1`)误判为越界 IP | 场景二 audit seq 10/12 两次 fail-closed,模型改写自救 | 误报打断流;WP-02 提取器新误报形态 | v1-P0:提取器改进 + 误判案例持续台账 |
| ⑧ | ENGAGEMENT.md「进展」占位文案与 loop.py:172 注释口径不一 | WP-06 模板 nit(WP-12 日志在册) | 文档口径不一致 | v1-P1:模板与注释口径统一 |
| ⑨ | sqlmap 类交互向导工具实弹零覆盖 | 场景一模型用 curl 手工完成注入;交互向导 PTY 适配仅 msfconsole 一个生产样本 | 交互向导面推广证据不足 | v1-P3:演示项(sqlmap 友好靶场;**WP-13 规格未含,留 M4 门口裁决**) |

## B. 各 WP 遗留在册(非挂账,日志已如实记录)

| # | 问题 | 现象与证据 | 影响 | v1 去向 |
|---|---|---|---|---|
| ⑩ | Claude 及 DeepSeek/GLM/OpenRouter 后端无凭据未实测 | WP-03 验收 4 🟡(核销表在册);Kimi K3 实测完整 | 多后端证据面单一 | v1-P2:多后端 failover 项并案补足实测 |
| ⑪ | 工具目录盘点漂移:Kali 补测 96/137 → WP-12 93/137 | 静态目录新鲜度随目标环境包集变化 | 地图覆盖率缓慢衰减 | v1-P3:目录维护/再校准机制 |
| ⑫ | 场景二观察到非本 run 的 msf 进程残留(外部环境观察) | WP-12 日志如实收录 | 非本产品缺陷,环境侧观察 | 无(信息项;v1 会话层可考虑环境感知,不单列) |

## C. WP-13 整合期新发现(文档级,代码零改动)

| # | 问题 | 现象与证据 | 处置 |
|---|---|---|---|
| ⑬ | HANDOVER 陷阱 15 路径表述过时:仓库现位于 `~/Desktop/foam`,条目仍记 `~/Documents/foam`(该目录已不存在) | WP-13 开工勘察实录;git 仓有效、HEAD 与台账一致 | append-only 纪律不动原条目;在 HANDOVER「当前状态」WP-13 条目内补记新路径 |
| ⑭ | 现 README 标题/类比行含显示名品牌串 2 处,与「显示名仅 pyproject + `__app_name__`」纪律冲突 | WP-13 品牌扫描 `git grep "Foam"` 实录 | **本 WP 内解决**:README 终版重写收口(零品牌串 + 命名纪律等价声明)。(2026-08-27 命名纪律退役:「违规」前提不再存在,README 同日已改为直书 Foam;本条仅作历史记录) |

## 附:扫描期确认为「非问题」的项(防止重复立项)

- `foam --help` 顶部出现显示名:经 `__app_name__` 常量渲染(cli.py:193-197),
  代码面零硬编码——品牌纪律的许可形态,非碎片化。(纪律已于 2026-08-27
  退役,此澄清仅作彼时记录。)
- 密钥扫描 3 处命中:全部为 TESTONLY 合成 fixture(test_backends.py:38/39、
  test_tui.py:83),契约明示允许。
- Mac 系统 python3=3.9.10 拒装:`requires-python>=3.12` 版本地板正确生效。
- Mac pip 直连需摘除失效代理 env:陷阱 4/20 族已在册,环境坑非产品缺陷。
