# WP-13 开发日志:总体整合 + 发布准备

> 关闭日期:2026-08-27。执行:WP-13 执行会话(零记忆起步,事实源=仓内文档)。
> 纪律:**全仓代码只读,本 WP 未改动任何代码文件一个字符**;关闭 commit 仅
> 文档面(README/docs/)。发现的缺陷/nit 全部进 `docs/known-issues.md` 或
> v1 立项清单,不在本 WP 修。

## 结果一览

| 验收条款 | 结论 |
|---|---|
| 1. 双场景 replay 双双通过哈希链校验 | ✅(56/49 条,双 exit 0,§验收1) |
| 2. 全新环境安装实测记录进日志 | ✅(Kali pipx + Mac venv 双平台,§验收2) |
| 3. 发布 checklist 逐项打勾或有 v1 去向 | ✅(§验收3 汇总表) |
| 4. 密钥扫描零命中(合成 fixture 除外) | ✅(当前树+全 29 commit 历史,§验收4) |
| 5. 中文开发日志 + 台账关闭 | ✅(本日志 + 台账 WP-13 行/关闭记录) |

产物:`docs/spec-verification.md`(63 条核销)、`docs/known-issues.md`、
`docs/demo/walkthrough.md` + `docs/demo/scenario2-report.md`、README 终版、
HANDOVER「v1 立项清单」节(14 项)、台账更新。**M4 门随之触发。**

## 设计取舍

1. **核对表口径**:每条款标 ✅/🟡/❌ + 证据代码(S 源码/T 测试/L 日志/
   E engagement/G 门口/K Kali 补测/W13 现场复核)。🟡 只给「条款本身达成但
   规格承诺面有偏差且有 v1 去向」的项——全表仅 WP-03④(其余后端无凭据
   未实测)一条;凡是「条款达成、另观察到新问题」的一律 ✅ 条款 + 问题进
   已知问题总表(如 WP-08④ 覆盖率实测完成 ✅,93/137 漂移进 ⑪)。
2. **README 品牌处理**:旧 README 标题/类比行含显示名 2 处,与「显示名
   仅 pyproject + `__app_name__`」纪律冲突。终版零品牌串,首行放命名纪律
   等价声明(即旧「代号可改」声明的等价表述);包名/命令/env 为定型标识符
   不受限。`foam --help` 顶部的显示名经 `__app_name__` 常量渲染
   (cli.py:193-197),属许可形态,不计碎片化(known-issues 附澄清)。
3. **演示材料取操作记录文本**(规格允许二选一):零新增依赖、命令可原样
   复演、素材直接用两场景 replay/report 输出,不重新实弹。
4. **安装证据组合**:Kali(pipx,目标平台)为主、Mac(干净 clone+venv)为
   对照;两处均补「安装后双场景 replay」冒烟,Kali 再加真实后端冒烟——
   验证的是**已安装包**而非仓内源码(pipx venv python 直调后端)。
5. **已知问题总表独立成文**(docs/known-issues.md),HANDOVER 只追加
   v1 清单节与状态条目,避免共享文件膨胀。

## 验收 1:双场景 replay 双双通过哈希链校验

命令(开发机,仓内):

```
$ PYTHONPATH=src .venv/bin/python -m foam.cli replay engagements/wp11-scenario1-20260825
[replay] engagements/wp11-scenario1-20260825:审计链完整,56 条记录,只读回放:
…(中间 54 条略,全文当日实录)…
[seq  56] 2026-08-25T07:52:11.492898+00:00 run 结束:finished,18 轮,tokens 输入 317641 / 输出 13095
exit=0

$ PYTHONPATH=src .venv/bin/python -m foam.cli replay engagements/wp12-scenario2-20260826
[replay] engagements/wp12-scenario2-20260826:审计链完整,49 条记录,只读回放:
…
[seq  49] 2026-08-26T09:24:07.142758+00:00 run 结束:finished,29 轮,tokens 输入 290166 / 输出 5227
exit=0
```

条数与 M3 监理独立复验记录(56/49)一致。replay 全文走查整理进
`docs/demo/walkthrough.md` §1/§2(关键 seq 对照表)。

## 验收 2:全新环境安装实测

### Kali(目标平台,主证据)

环境:Kali rolling arm64,Python 3.13.7,pipx 1.7.1(/usr/bin/pipx)。
源:开发机 `git archive -o /tmp/foam-head.tar.gz HEAD`(fb1b870)→ scp →
Kali **干净目录** `/home/doma/foam-install-test/` 解出(~/foam rsync 副本
全程未碰;Kali 无 proxy env):

```
doma@kali$ pipx install /home/doma/foam-install-test
  installed package foam 0.0.1, installed using Python 3.13.7
  These apps are now globally available
    - fm
    - foam
doma@kali$ ~/.local/bin/foam --help | head -2
usage: foam [-h] [--version] {run,resume,replay,report,tui} ...
doma@kali$ ~/.local/bin/fm --version
Foam 0.0.1
```

安装后冒烟一(双场景 replay,engagement 目录 scp 至 /home/doma/eng-smoke/):

```
s1-exit=0  [replay] …/wp11-scenario1-20260825:审计链完整,56 条记录,只读回放:
s2-exit=0  [replay] …/wp12-scenario2-20260826:审计链完整,49 条记录,只读回放:
```

安装后冒烟二(真实后端,**经已安装包**直调:pipx venv 的 python 跑一段
10 行脚本,`OpenAICompatBackend` + `Message.user("只回答两个字:就绪")`,
env 取 `~/.zshenv` 的 FOAM_LLM_BASE_URL/FOAM_LLM_MODEL/FOAM_LLM_API_KEY
三变量——只验存在性,全程未打印 key):

```
first-event=6.73s total=8.03s reply='就绪' reasoning_chars=142
usage: in=91 out=47
```

真实 LLM 冒烟的 key 按总指挥指示找到:Kali 侧 `~/.zshenv` 已配好 3 个
FOAM 导出(WP-12 遗产,陷阱 23 落点);Mac 侧 env 无 FOAM 变量(本 WP
未在 Mac 使用真实后端)。Kali 侧的 pipx 安装与 eng-smoke 目录保留在盘上,
供 M4 门口演示直接用。

### Mac(对照组)

```
$ git clone -q --depth 1 file:///Users/an/Desktop/foam /tmp/foam-clean-mac
$ /opt/homebrew/bin/python3.12 -m venv /tmp/foam-venv-mac   # Python 3.12.14
$ env -u ALL_PROXY -u all_proxy -u http_proxy -u https_proxy … \
    /tmp/foam-venv-mac/bin/pip install -q /tmp/foam-clean-mac
$ /tmp/foam-venv-mac/bin/pip list | grep -E '^(foam|httpx|rich|textual) '
foam 0.0.1 / httpx 0.28.1 / rich 15.0.0 / textual 8.2.8
$ /tmp/foam-venv-mac/bin/foam replay engagements/wp11-scenario1-20260825 → 56 条 exit 0
$ /tmp/foam-venv-mac/bin/foam replay engagements/wp12-scenario2-20260826 → 49 条 exit 0
```

**失败形态如实收录两则**(均属环境坑,非产品缺陷):
① 首次用系统 `python3`(=**3.9.10**)建 venv,构建阶段失败——
`requires-python>=3.12` 版本地板正确生效;② 默认 shell 带失效代理
env(ALL_PROXY 指向死 SOCKS,陷阱 4/20 族),pip 拉 hatchling 失败,
摘除代理 env 后直连装通。

**口径说明**:安装实测基于 fb1b870(本 WP 关闭前最后一个 commit);
关闭 commit 仅文档面改动,不影响安装行为。关闭后已按计划以最终 HEAD
重验(见文末「关闭后复验」节)。

## 验收 3:发布 checklist 汇总表

| 检查项(规格点明) | 结论 | 证据/去向 |
|---|---|---|
| 全新 Kali 机 `pipx install` 实测 | ✅ | §验收2 Kali 节 |
| 依赖清单审计(进 Kali 源可行性) | ✅ 可行 | §依赖审计:运行时 3 依赖 Kali 源全有且版本满足约束 |
| GPL 头/许可证完整 | ✅ | LICENSE=GPL-3.0 全文,pyproject `license="GPL-3.0-only"` + `license-files` 一致;per-file 头:全仓无(grep 实录)——仓库级单 LICENSE 策略对发布完备(GPL 不强制 per-file 头),不单列 v1;若总指挥要 per-file 头,v1 一句话可加 |
| `.gitignore` 复核 | ✅ | `git ls-files` 对照运行时产物模式(engagements//*.sqlite/.env/.venv/__pycache__/.log/loot)零命中;engagements/、.env、*.local.scope 均在列 |
| 密钥扫描 | ✅ | §验收4 |
| 品牌名占位复核 | ✅ | §品牌复核:显示名仅许可两处+元记录 5 处,README 2 处已收口;包名/命令/env 零碎片化 |
| 双场景回放走查 + 演示材料 | ✅ | docs/demo/(文本路线,README 已链接) |
| 13 份规格 vs 实现逐条核销 | ✅ | docs/spec-verification.md(63 条) |
| 已知问题总表 | ✅ | docs/known-issues.md(14 条+非问题澄清 4) |
| v1 立项清单进 HANDOVER | ✅ | HANDOVER「v1 立项清单」节,14 项 P0–P3 |
| M1–M3 门口记录齐全(M4 补充条款) | ✅ | 核销表末节:台账门表四行 × HANDOVER 门口节 × 证据链逐一点名,无缺口 |

## 验收 4:密钥扫描零命中

模式集(高信号):`sk-*`/`ghp_*`/`github_pat_*`/`gho_*`/`AKIA*`/`xox[baprs]-*`/
`AIza*`/`BEGIN * PRIVATE KEY`;另扫 JWT(`eyJ…`)、`FOAM_LLM_API_KEY=<值>`、
宽模式 `(api_key|secret|passwd|password|token)[:=] '<8+字符>'`。

```
当前树(git grep):唯一命中 3 处,全为 TESTONLY 合成 fixture——
  tests/test_backends.py:38  FAKE_OAI_KEY = "sk-TESTONLY-fake0000"
  tests/test_backends.py:39  FAKE_CLAUDE_KEY = "sk-ant-TESTONLY-fake0000"
  tests/test_tui.py:83       FAKE_KEY = "sk-TESTONLY-…"(行内注释:合成,非真实 key)
历史:全 29 个 commit 逐个 git grep 同模式,命中同样只有上述 fixture 行
  (随文件引入/演进出现于 24 个 commit 的同位置,无一次真实 key)。
JWT:零命中。FOAM_LLM_API_KEY=<值>:唯一命中 docs/e2e/scenario2-setup.md:52
  的 `<env 一次性导出>` 文档占位符。
宽模式:src 仅 2 处 env 名常量(ENV_API_KEY = "FOAM_LLM_API_KEY" 等,
  是变量名不是值);tests 1 处 session 测试里的 'admin password: ' 提示文本。
```

结论:**零命中(合成 fixture 3 处按契约豁免,逐处点名)**。engagements/
为 gitignored 运行时产物不在发布面;LLM key 只走 env,审计链对 LLM 内容
只记哈希(WP-02 设计),冒烟全程未打印 key。

## 验收 5:中文开发日志 + 台账关闭

本日志;台账 WP-13 行 pending→closed(2026-08-27)+ 关闭记录条目追加;
HANDOVER「当前状态」WP-13 条目、「下一 WP」收口、「v1 立项清单」新节。
M4 门表行保持「待触发」——门口动作是协调层动作,留总指挥(沿 WP-12
对 M3 的同款处理)。

## 测试基线与 lint(关闭前终跑)

```
$ .venv/bin/python -m pytest -q
.........................                          [100%]
383 passed, 2 skipped in 43.08s            # 2 skip = WP-05 ssh/msfconsole Mac 环境门控(已经 Kali 核销)
$ .venv/bin/python -m ruff check .
All checks passed!
```

## 品牌占位复核实录

`git grep "Foam"`(排除 pyproject.toml 与 src/foam/__init__.py):
AGENTS.md:8(§0 纪律原文)、docs/HANDOVER.md:74/77/308(定名与搬家历史
条目)、docs/work-packages/WP-13.md:33(规格排除项引文)、旧 README:1/8
——分类:许可 2 处(pyproject `name`/`description` 与 `__app_name__`)+
元记录 5 处(纪律原文与定名历史,指名是必须的)+ README 2 处(**本 WP
终版已收口**,终版 grep 零命中)。旧代号「Kali Code」「The Form」仅存在于
定名历史条目;`git grep -in 'kali.code'` 非历史文档零命中。`src/` 内
FOAM_* env 恰好 4 个(API_KEY×2/BASE_URL/MODEL),无碎片化。

## 依赖审计实录

pyproject 运行时依赖 3 条,dev 3 条。Kali rolling 实机 `apt-cache policy`
(LC_ALL=C,首次查询撞中文 locale「候选:」致 awk 落空,换 C locale 复得):

```
python3-httpx        => 0.28.1-1.1   (约束 >=0.27  ✓;机上已装 0.28.1-1)
python3-rich         => 15.0.0-1     (约束 >=13.7  ✓;已装 13.9.4-1)
python3-textual      => 8.2.3-1      (约束 >=0.86  ✓)
python3-pytest       => 9.1.1-1      (dev,>=8.0    ✓)
python3-pytest-asyncio => 1.4.0-2    (dev,>=0.23   ✓)
ruff                 => (none)       (dev-only lint 工具,非测试门槛,
                                      不影响打包;pipx 可另装)
```

结论:运行时三依赖全部在 Kali 官方源且版本满足约束,「进 Kali/Debian
官方源」的长期目标在依赖面可行;构建后端 hatchling 为打包侧工具(Debian
有 python3-hatchling 系,打包时按 Debian 惯例处理)。

## 13 份规格核销方法与本 WP 自我纠错

方法:读全部 13 份规格抽出 63 条验收条款 → 逐条配证据(源码符号 grep
核实:audit.verify:205、parse.maybe_parse:112/apply_facts:165、
session.PROMPT_PATTERNS:118、toolmap scan_tools/render_tool_map/
render_startup_line:163/256/278、cli.py `__app_name__` 渲染:193-197、
tui banner/app 经 `__app_name__`、test 文件 15 个 280 测试函数)→ 定量
条款现场复测(WP-02④:append 10k 条 0.118s、verify True 0.058s)→
engagement 产物 sqlite/grep 对账。
**自我纠错一则**:核销表初版把场景二 loot 文件名写成
`msf-unrealircd-root.txt`(凭记忆),对账时发现盘上实为
`msf-unrealircd-session.log`,已更正两处并复核 `uid=0(root)` 命中
行 42/70 与台账记录逐字一致——台账原文未含文件名,仓内记录无错,
是我方草稿之误;教训:证据行逐字对账,不凭记忆写文件名。

## M1–M3 门口记录齐全核对(M4 补充条款)

台账「里程碑门」表四行 × HANDOVER「里程碑门」节 × 证据链(WP-04 日志
「补跑」节:195 起 / WP-09 日志「更正当」节:218 起,commit 4c19e02 /
M3 监理复验记录)逐一点名:M1 演示+放行齐、M2 更正当+演示打分+放行齐、
M3 复验+复评+放行+挂账移交齐、M4 待触发(本 WP 关闭即触发)。
三门口产出均在「放行/更正当/砍 WP」三选一内,无开新坑。**记录齐全,
无缺口,无需先补门口动作。**

## M3 挂账 9 条处置(候选非验收项)

9 条全部作为 v1 立项素材按优先级排进 HANDOVER「v1 立项清单」节
(①→P1-4、②→P1-5、③→P1-7、④⑤→P0-2 并案、⑥→P1-6、⑦→P0-3、
⑧→P1-6、⑨→P3-12),并在核销表落位到相关条款旁注、在
known-issues.md 逐条建档。**未动任何代码与验收口径**。挂账⑨(sqlmap
演示)规格未含,按监理口径**未做**,与 ④⑥ 两项门口定夺一并留 M4 裁决。

## 踩坑(本 WP 新踩/重踩,回填价值自评)

1. 重踩陷阱 13 两连:`echo ====BRAND---===` 被 zsh 当命令解析(=开头
   的 equals expansion)致整链中断;`cmd | tail; echo $?` 又打到 tail
   退出码(记录 pip 结果时改用产物验证法:pip list + foam --version +
   replay,而非 $?)。
2. Kali `apt-cache policy` 输出是中文 locale(候选:/已安装:),英文
   awk 模式落空显示全 MISSING——`LC_ALL=C` 后复得。跨机脚本模式匹配
   先考虑 locale。
3. Mac 对照组两坑实录:系统 python3=3.9.10(版本地板正确拒装,反而
   成了地板生效的证据);默认代理 env 失效(陷阱 4/20 族)需摘除后
   pip 直连。安装类验证脚本:先报解释器版本再谈装。
4. 凭记忆写证据文件名翻车(见「自我纠错」)——证据引用一律对账。

## 新发现与 nit(全部进 known-issues.md,不在本 WP 修)

- ⑬ HANDOVER 陷阱 15 路径过时(`~/Documents/foam` → 现
  `~/Desktop/foam`):append-only 不动原条目,已在 HANDOVER「当前状态」
  WP-13 条目补记。
- ⑭ 旧 README 品牌串 2 处:本 WP 终版已收口(文档级修正,规格允许)。
- 其余非问题澄清 4 条(help 品牌串来源/3 处 fixture/py3.9 拒装/代理坑)
  见 known-issues.md 附录,防重复立项。

## 本 WP 文件清单(关闭 commit 全部内容)

- 新增:`docs/spec-verification.md`、`docs/known-issues.md`、
  `docs/demo/walkthrough.md`、`docs/demo/scenario2-report.md`、
  `docs/dev-logs/WP-13.md`(本文件)
- 修订:`README.md`(终版)、`docs/HANDOVER.md`(当前状态/下一 WP/
  v1 立项清单节)、`docs/wp-ledger.md`(WP-13 行 + 关闭记录)
- 代码:零改动。engagement 产物零入库(git status 自查)。

## 关闭后复验(最终 HEAD 安装确认)

关闭 commit(dc1d5df,纯文档面)落盘后,以最终 HEAD 重新
`git archive` → Kali 干净目录 `/home/doma/foam-final-check/` →
`pipx install --force` 重装 → 冒烟:

```
installed package foam 0.0.1(apps: fm, foam)
$ foam --version → Foam 0.0.1
$ foam replay eng-smoke/wp11-scenario1-20260825 → 审计链完整,56 条记录,exit 0
```

最终发布 commit 与安装实测基线(fb1b870)差异仅在文档面,安装行为
逐字节一致的预期得到确认。本节的回填本身不改变任何验收证据(全部
验收动作在 commit 前完成并实录如上)。
