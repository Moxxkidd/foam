# WP-13:总体整合 + 发布准备(最终整合 WP)

状态:pending|依赖:全部 WP 关闭

## 目标

所有 WP 落地后的总收口:

- **文档收口**:HANDOVER/台账/AGENTS.md 终态核对;13 份规格 vs 实现
  逐条核销;已知问题总表;README 终版(特性、安装、quickstart、双场景
  结果摘要、法律声明)。
- **演示材料**:双场景回放走查 + 终端录屏(asciinema)或操作记录文本,
  链接进 README。
- **发布 checklist**:全新 Kali 机 `pipx install` 实测;依赖清单审计
  (进 Kali 源的可行性);GPL 头/许可证完整;`.gitignore` 复核;密钥扫描;
  品牌名占位复核(显示名只动 pyproject + `__init__.py` 常量;包名
  `theform`/命令 `theform`/`tf`/env `THEFORM_*` 已于 2026-08-22 定名
  定型,复核其无碎片化)。
- **v1 立项清单**:nftables 网络级护栏、多后端 failover、报告模板化、
  meterpreter 语义层、经验库……按优先级排列,写进 HANDOVER。
- 本 WP 只读代码;发现缺陷只记录并开 v1 清单,不在本 WP 修(文档级
  修正除外)。

**补充(2026-08-21,里程碑门挂牌)**:本 WP 即 M4 门(发布评审)。
整合验收时须核对 M1–M3 的门口记录齐全(演示证据 + 放行/更正当/砍 WP
结论,见 `docs/wp-ledger.md`「里程碑门」);任何一门缺记录,先补门口
动作再关闭本 WP。

## 排除项

- 任何新功能。
- 改名执行(**已于 2026-08-22 完成**:暂用代号「Kali Code」→ 正式名
  「The Form」;本条自此无对象,仅品牌名占位复核仍属 checklist)。

## 涉及文件

- 全仓只读 + `README.md`、`docs/`(整合产物)。

## 验收标准

1. 双场景 replay 双双通过哈希链校验。
2. 全新环境安装实测记录进日志。
3. 发布 checklist 逐项打勾或有 v1 去向。
4. 密钥扫描零命中(合成 fixture 除外)。
5. 中文开发日志 + 台账关闭。
