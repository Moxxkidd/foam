# 授权范围(scope)文件示例——本地实验靶场
#
# 每行一个目标,支持三种写法:
#   CIDR       192.168.56.0/24
#   主机/域名   localhost / metasploitable.testlab.local(支持 *. 前缀通配)
#   URL 前缀    http://127.0.0.1:3000/
#
# 以 # 开头为注释,空行忽略。
#
# 红线:只有你对目标拥有明确授权时才能把它写进 scope。
# scope 之外的命令会被护栏拒绝并记入审计链。

# 本地回环(容器靶场默认)
127.0.0.0/8
localhost
http://127.0.0.1:3000/

# 本地虚拟网段(Metasploitable2 等 VM 靶场常见段,按实际改)
192.168.56.0/24
